{
  "name": "pariwartan-portfolio",
  "version": "1.0.0",
  "description": "Professional portfolio website with Django backend and React frontend",
  "scripts": {
    "dev": "concurrently \"python manage.py runserver\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "backend": "python manage.py runserver",
    "frontend": "cd frontend && npm start",
    "install-all": "pip install -r requirements.txt && cd frontend && npm install"
  },
  "keywords": ["portfolio", "django", "react", "python", "web-development"],
  "author": "Pariwartan Poudel",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}